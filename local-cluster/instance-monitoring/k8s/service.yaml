apiVersion: v1
kind: Service
metadata:
  name: instance-monitoring
  namespace: monitoring
spec:
  type: ClusterIP
  selector:
    app: instance-monitoring
  ports:
    - name: dashboard
      port: 5701
      targetPort: 5701
